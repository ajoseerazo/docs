---
title: "Crear payin con código QR"
api: "POST https://stablecoin-api.sandbox.getmeru.com/v1/payins/qr-code"
description: "Genera un código QR y crea un payin asociado a ese QR."
---

### Cuerpo

<ParamField body="externalId" type="string">
  Id de referencia del cliente (debe ser único por empresa). Si se omite, usa <code>orderId</code> por defecto.
</ParamField>

<ParamField body="orderId" type="string" required>
  Identificador de orden del cliente.
</ParamField>

<ParamField body="amount" type="number" required>
  Monto a pagar.
</ParamField>

<ParamField body="taxAmount" type="number">
  Porción de impuestos (si aplica).
</ParamField>

<ParamField body="currency" type="string">
  Código de moneda (por defecto <code>USD</code>).
</ParamField>

<ParamField body="expireAt" type="string">
  Fecha y hora de expiración del QR (ISO-8601).
</ParamField>

<ParamField body="onBehalfOf" type="string">
  Referencia del usuario/cliente.
</ParamField>

### Respuesta

<ResponseField name="success" type="boolean">
  Indica si el payin por QR se creó correctamente.
</ResponseField>

<ResponseField name="paymentId" type="string">
  El identificador único del pago generado por nuestro sistema.
</ResponseField>

<ResponseField name="qrCode" type="string">
  La imagen del código QR (data URL en base64).
</ResponseField>

<ResponseField name="expireAt" type="string">
  La fecha y hora (ISO 8601) en que expira el código QR.
</ResponseField>



<RequestExample>

```bash cURL
curl --location --request POST 'https://stablecoin-api.sandbox.getmeru.com/v1/payins/qr-code' \
--header 'Content-Type: application/json' \
--header 'api-key: <api-key>' \
--data-raw '{
    "externalId": "EXT-10001",
    "orderId": "ORD-12345",
    "amount": 99.99,
    "taxAmount": 8.99,
    "currency": "USD",
    "expireAt": "2026-04-17T12:46:44.310Z",
    "onBehalfOf": "cus_123"
}'
```

```js Javascript
const response = await fetch('https://stablecoin-api.sandbox.getmeru.com/v1/payins/qr-code', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'api-key': '<api-key>'
  },
  body: JSON.stringify({
    externalId: 'EXT-10001',
    orderId: 'ORD-12345',
    amount: 99.99,
    taxAmount: 8.99,
    currency: 'USD',
    expireAt: '2026-04-17T12:46:44.310Z',
    onBehalfOf: 'cus_123'
  })
});

const data = await response.json();
```

</RequestExample>

<ResponseExample>

```json 200
{
  "success": true,
  "paymentId": "pay_3ddd0e5b-6276-4b48-b756-c1fcc9a2efd1",
  "qrCode": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA...",
  "expireAt": "2026-04-17T12:46:44.310Z"
}
```

</ResponseExample>

### Respuestas de error

<ResponseExample>

```json 409
{
  "success": false,
  "error": "externalId was already used"
}
```

</ResponseExample>
